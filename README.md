# EdgeDock launch site

Static GitHub Pages site for EdgeDock Beta 0.1.

The support button intentionally remains disabled until the creator supplies the final Ko-fi URL. The Windows ZIP in `downloads/` is generated from the verified `publish` output in the private application repository.
